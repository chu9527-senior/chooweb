# DESIGN.md — 인생설계가이드(추영준) 홍보 웹

> 이 문서는 현재 `index.html`에 이미 구현된 디자인을 기준(source of truth)으로 정리한 것입니다.
> 앞으로 이 사이트의 화면을 만들거나 고칠 때는 여기 적힌 토큰과 규칙을 따릅니다.
> 새 규칙이 필요하면 코드보다 먼저 이 파일을 고칩니다.

---

## 0. Design Read (한 줄 정의)

개인 브랜드 랜딩(강연·저서 홍보) / 대상은 강연 섭외 담당자와 중장년 개인 /
**차분하고 신뢰감 있는 에디토리얼** 언어 / 네이비 + 골드 on 웜페이퍼, Pretendard, 절제된 모션.

- **페이지 종류:** 원페이지 랜딩 (소개형 퍼스널 브랜딩)
- **톤앤매너:** 신뢰감 있고 따뜻한 전문가 이미지. 중장년 독자를 배려한 차분한 톤.
- **브랜드 닉네임:** **인생설계가이드** (다른 명칭 사용 금지)
- **quiet constraints:** 중장년 가독성 우선 → 본문 대비/글자 크기 넉넉하게, 화려한 인터랙션 금지

## 1. 다이얼

| 다이얼 | 값 | 의미 |
|---|---|---|
| DESIGN_VARIANCE | 4 | 거의 대칭, 정돈된 레이아웃 |
| MOTION_INTENSITY | 2 | 거의 정적. hover 색 변화 + `:active` 눌림 정도만 |
| VISUAL_DENSITY | 4 | 여백 있으나 정보는 또렷하게 |

이유: "trust-first / 중장년 / 공공적 신뢰" 성격이라 실험적 요소를 배제한다.

---

## 2. 스택 (바꾸지 말 것)

- **순수 HTML + CSS + Vanilla JS.** 프레임워크·빌드 도구 없음.
- 배포: hosting.kr 리눅스 cPanel + PHP (`contact.php`, `admin.php`). 정적 호스팅 + PHP 폼 처리만으로 동작해야 함.
- CSS는 `index.html` 안 `<style>` 블록에 인라인. 별도 CSS 파일로 쪼개지 않는다.
- 폰트: **Pretendard** (jsDelivr CDN, `pretendard@v1.3.9`). 폴백: `-apple-system, BlinkMacSystemFont, 'Malgun Gothic', sans-serif`.
- React/Tailwind/Motion/GSAP/아이콘 라이브러리 **도입 금지.** (design-taste-frontend 스킬의 기본 스택 권장을 여기서는 따르지 않는다 — "기존 프로젝트 우선" 원칙.)
- 아이콘: 현재는 한글 한 글자(학·경·현·수)를 원형 뱃지에 넣는 방식. 아이콘이 더 필요하면 인라인 SVG를 직접 넣되 단순 기하 도형만.

---

## 3. 색 (CSS 변수, `:root`에 이미 정의됨)

| 변수 | 값 | 용도 |
|---|---|---|
| `--color-bg` | `#faf7f2` | 기본 배경 (웜 페이퍼) |
| `--color-bg-alt` | `#f1ece2` | 교차 섹션 배경, 태그 배경 |
| `--color-navy` | `#1c2b45` | 제목, 1차 버튼, 푸터, 강조 카드 |
| `--color-navy-light` | `#324566` | 네이비 hover, 그라데이션 |
| `--color-gold` | `#b8934a` | 섹션 라벨, 링크 강조, 포커스 링 |
| `--color-gold-light` | `#d8b978` | 어두운 배경 위 제목, 그라데이션 |
| `--color-text` | `#2b2b2b` | 본문 |
| `--color-text-muted` | `#6b6b6b` | 보조 설명 |
| `--color-white` | `#ffffff` | 카드, 밝은 섹션 |

규칙:
- **악센트는 골드 하나로 고정.** 페이지 어디서도 다른 악센트 색(파랑 글로우, 네온 등)을 새로 만들지 않는다.
- 상태 메시지 색은 예외적으로 허용: 성공 `#e7f4ea`/`#276b3a`, 오류 `#fbeaea`/`#a33a3a`.
- 그림자는 네이비 틴트만: `--shadow: 0 10px 30px rgba(28,43,69,0.08)`. 순수 검정 그림자 금지.
- 페이지 테마는 **라이트 고정.** 섹션별로 반전하지 않는다. (네이비 카드/푸터는 "어두운 블록"이지 다크모드가 아니다.)

---

## 4. 타이포그래피

| 역할 | 크기(모바일 → ≥768px) | 두께 | 색 |
|---|---|---|---|
| Hero 타이틀 (`.hero-title`) | 2.2rem → 2.7rem | 800 | navy (강조어 `em`은 gold, `font-style:normal`) |
| 섹션 타이틀 (`.section-title`) | 1.8rem → 2.1rem | 800 | navy |
| 섹션 라벨 (`.section-label`) | 0.85rem | 700 | gold, `text-transform:uppercase`, `letter-spacing:0.12em` |
| 섹션 설명 (`.section-desc`) | 1rem | 400 | text-muted, `max-width:640px` |
| 본문 | 1rem, `line-height:1.7` | 400 | text |
| 카드 소제목 | 0.98–1.15rem | 700 | 문맥에 따라 navy 또는 gold-light |
| 캡션/태그/폼 라벨 | 0.82–0.92rem | 600–700 | navy 계열 |

- 전역 `word-break:keep-all` (한글 단어 중간 줄바꿈 방지). 유지할 것.
- 강조는 **같은 폰트의 색/두께 변화**로만. 세리프 섞기 금지, 이모지 금지.
- **섹션 라벨(eyebrow) 절제:** 현재 모든 섹션에 영문 라벨이 붙어 있다. 섹션을 추가할 때는 3개 섹션당 1개 이하로 줄이는 방향으로 간다. 새 섹션에 무조건 라벨을 달지 않는다.

---

## 5. 레이아웃 & 스페이싱

- 컨테이너: `--max-width: 1120px`, 좌우 패딩 20px, `margin:0 auto`.
- 섹션 세로 패딩: 72px(모바일) → 96px(≥768px). Hero는 `64px 0 56px` → `96px 0 80px`.
- 브레이크포인트: **768px**(태블릿/데스크톱), **1024px**(Hero 가로 정렬, 저서 4열). `sm/md/lg` 외 커스텀 브레이크포인트 만들지 않는다.
- 그리드 우선. flex 퍼센트 계산(`calc(33% - ...)`) 금지.
  - About: 1열 → 2열(≥768)
  - 저서: 2열 → 3열(≥768) → 4열(≥1024)
  - 강연: 1열 → 2열(≥768)
  - Contact: 1열 → `0.9fr 1.1fr`(≥768)
- Hero: 모바일 세로 중앙정렬 → ≥1024px에서 좌우 배치(`.hero-copy` 좌, 원형 사진 우).
- 네비게이션 높이 **68px 고정.** 데스크톱에서 한 줄. 모바일은 햄버거(현재 구현 유지).

---

## 6. 컴포넌트 규칙

### 버튼 (`.btn`)
- 모양: **풀 필(`border-radius:999px`)**, 패딩 `14px 28px`, 두께 700.
- 종류 2개만: `.btn-primary`(navy 배경/흰 글자, hover navy-light), `.btn-outline`(투명/네이비 테두리, hover 반전).
- 피드백: `:active` 시 `transform:scale(0.97)`. 그 외 모션 없음.
- 라벨은 데스크톱에서 한 줄에 들어와야 함. 1차 CTA는 2~3단어.
- **CTA 인텐트 통일:** 강연 섭외 = "강연 문의하기" 한 표현으로. ("문의하기", "상담 신청" 등으로 흔들지 않는다.)

### 카드
- 모서리 반경 `--radius: 14px` 로 통일. (버튼=999px, 폼 인풋=10px, 책 표지=8px 는 이미 정해진 예외 규칙.)
- 카드 그림자는 `--shadow` 하나만.
- 카드는 "높이 차이가 실제 위계를 나타낼 때"만 사용. 아니면 `border` + 배경 틴트로 묶는다 (`.fact-item` 방식).
- 강조 카드(`.topic-card`)는 navy 배경 + gold-light 제목. 대체 버전은 `.topic-card.alt`(bg-alt).

### 폼 (`#contactForm`)
- 라벨은 **인풋 위**, 필수는 `*`(`aria-hidden`) 표시. placeholder를 라벨 대신 쓰지 않는다.
- 인풋: 반경 10px, 배경 `--color-bg`, 테두리 `rgba(28,43,69,0.18)`.
- 포커스: `outline:none` + `border-color:gold` + `box-shadow:0 0 0 3px rgba(184,147,74,0.18)`.
- 상태 메시지(`#formMsg`)는 폼 상단, `role="status"`. 성공/오류 색은 §3 참고.
- 허니팟 필드(`.hp-field`) 유지. 봇 방지용 히든 필드이므로 지우지 않는다.
- 하단에 이메일 직접 문의 폴백(`mailto:`) 문구 유지.

### 이미지
- 프로필: 원형(`border-radius:50%`), 220px, 흰 테두리 6px, `--shadow`, `object-fit:cover`.
- 책 표지: `max-width:150px`, 반경 8px, 그림자 `0 8px 20px rgba(28,43,69,0.18)`.
- 모든 `img`에 의미 있는 한글 `alt`. 장식용 순수 CSS 도형(예: `.book-spine`)에는 alt 불필요.
- **실사진 우선.** 자리표시자 이미지는 배포 전 실제 사진으로 교체 (TODO 항목).

---

## 7. 접근성 (중장년 대상 → 강제 규칙)

- 본문 대비 WCAG AA(4.5:1) 이상. muted 텍스트도 `#6b6b6b` 아래로 더 흐리게 하지 않는다.
- 버튼/링크 글자와 배경 대비 확인. 골드(`#b8934a`) 위 흰 글자는 대비 부족 → 골드는 배경이 아니라 텍스트/포인트로만.
- 포커스 링 항상 보이게(현재 골드 글로우). `outline:none`만 남기지 않는다.
- `aria-label`, `aria-expanded`(햄버거), `role="status"`(폼 메시지) 유지.
- `scroll-behavior:smooth`는 유지하되 큰 폭의 패럴럭스/스크롤 하이재킹 금지.

---

## 8. 하지 말 것 (이 프로젝트 한정)

- 프레임워크/빌드 도구/CSS 파일 분리
- 다크모드, 섹션별 테마 반전
- 골드 외 새 악센트 색, AI 보라/네온 그라데이션
- 세리프 폰트, 이모지, 손으로 그린 장식 SVG 일러스트
- 무한 루프 애니메이션, 마퀴, 스크롤 핀/스크럽
- placeholder를 라벨로 쓰기, 카드 안에 카드 3중 중첩
- 브랜드명을 "인생설계가이드" 외 다른 말로 쓰기
- 경력을 "기자"로 표기 (정확히는 "사업부장")

---

## 9. 검수 체크리스트 (배포 전)

- [ ] 새 섹션의 색/반경/그림자가 §3·§6 토큰과 일치
- [ ] 악센트가 골드 하나로 유지됨
- [ ] 모든 CTA 라벨이 데스크톱 한 줄, "강연 문의하기"로 통일
- [ ] 폼 라벨 위치·포커스 링·대비 통과
- [ ] 모든 이미지 `alt` 존재, 자리표시자 교체 완료
- [ ] 768 / 1024 브레이크포인트에서 레이아웃 깨짐 없음
- [ ] 중장년 기준 본문 가독성(대비·크기) 점검
- [ ] `web-design-guidelines` 스킬로 최종 UI 감사
